version: '3.8'

services:
  app-test:
    container_name: app-test
    environment:
      - TZ=Europe/Moscow
    build:
      context: .
      dockerfile: Dockerfile-tests
    volumes:
      - ./tests/api.conf
    command: python3 -m pytest --disable-pytest-warnings

version: '3.8'

services:
  app:
    environment:
      - TZ=Europe/Moscow
    build: .
    restart: unless-stopped
    command: ./run-api /config/api.conf
    volumes:
      - config:/config
      - log:/log
    ports:
      - ${APP_PORT_EXT}:5000
volumes:
  config:
  log:

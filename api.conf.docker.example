[DATABASE]
HOST = 10.0.0.1
NAME = repodb
TRY_NUMBERS = 5
TRY_TIMEOUT = 5
USER = default
PASSWORD =

[APPLICATION]
HOST = 0.0.0.0
PORT = 5000
PROCESSES = 0
TIMEOUT = 60
CORS_ORIGINS = "*"

[OTHER]
ADMIN_USER = admin
ADMIN_PASSWORD = 
LOG_FILE = /log/api.log
LOG_LEVEL = 3            # 0 : critical, 1 : error, 2 : warning, 3 : info, 4 : debug
SQL_DEBUG = false
LOG_TO_FILE = true
LOG_TO_SYSLOG = false    # usually not avallable inside container
LOG_TO_CONSOLE = false

[LDAP]
LDAP_SERVER_URI =
LDAP_USER_SEARCH = uid=%%(user)s,cn=users,cn=accounts,dc=ipa,dc=example,dc=com
LDAP_REQUIRE_GROUP = cn=%%(group)s,cn=groups,cn=accounts,dc=ipa,dc=example,dc=com

[GROUPS]
API_ADMIN =
API_USER =
CVE_ADMIN =
CVE_USER =

[AUTHENTICATION]
TOKEN_STORAGE = file
EXPIRES_ACCESS_TOKEN = 300 # 5 * 60
EXPIRES_REFRESH_TOKEN = 43200 # 12 * 60 * 60
MAX_REFRESH_SESSIONS_COUNT = 3

[REDIS]
REDIS_URL =

[ERRATA]
ERRATA_ID_URL =
